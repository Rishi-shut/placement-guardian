name: placement-guardian-bot
region: eu
build:
  dockerfile: backend/Dockerfile
  context: .
run:
  web: uvicorn app.main:app --host 0.0.0.0 --port $PORT
  type: web
env:
  - key: PYTHONUNBUFFERED
    value: "1"
